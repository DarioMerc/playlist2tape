{
  "name": "playlist2tape",
  "version": "1.0.0",
  "description": "See if a spotify mixtape will fit on a cassette tape",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "frontend": "cd frontend && npm run serve",
    "backend": "nodemon server.js",
    "build": "cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dario Mercuri",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "sass": "^1.72.0",
    "sass-loader": "^14.1.1"
  }
}
